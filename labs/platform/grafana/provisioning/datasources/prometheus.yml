# Grafana datasource provisioning - Acme labs.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: acme-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
